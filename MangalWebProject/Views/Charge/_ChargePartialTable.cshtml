@model IList<MangalWebProject.Models.ChargeViewModel>
@using MangalWebProject.Models
<div class="modal-content">
    <div class="modal-body" style="height:inherit; overflow:auto;">
        <div class="panel-body panel-no-padding" style="height:inherit; overflow:auto;">
            <div id="demo">
                <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-hover table-fixed-header m0" id="example">
                    <thead>
                        <tr>
                            <th>Charge Name</th>
                            <th>Reference Date </th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (ChargeViewModel item in Model)
                        {
                            <tr class="gradeX" onclick='window.document.location="/Charge/GetChargeDetailsById?ID=@item.ID"'>
                                <td>@item.ChargeName</td>
                                <td>@item.ReferenceDate.ToShortDateString()</td>
                                <td>@item.StatusStr</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div><!-- /.modal-content -->

